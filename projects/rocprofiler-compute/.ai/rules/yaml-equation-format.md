# YAML Metric Equation Formatting

The YAML equation formatting guide for this project lives in **[`YAML_EQUATION_FORMAT.md`](../../YAML_EQUATION_FORMAT.md)**. Each section there describes the canonical format alongside worked examples.

When generating or modifying metric equations in YAML config files, read the relevant section before writing.
